# 构建上下文裁剪：以下内容会在镜像内重新安装/生成，或运行时用不到。

# 依赖与构建产物（builder 内重新 npm ci / next build）
node_modules
.next
out
build
coverage

# 版本控制 / CI 配置
.git
.gitignore
.github

# 本地密钥与环境文件（绝不进镜像）。覆盖 .env / .env.local / .env.production / .env.staging 等所有变体；
# 仅放行只含占位符的 .env.example。
.env
.env.*
!.env.example

# 本地对象存储 / 上传
.minio
uploads

# 编辑器 / 系统
.DS_Store
.vscode
.idea
*.tsbuildinfo
*.pem

# 文档与产品资料（运行时不需要）
docs
*.md
PRDv0.3.md
